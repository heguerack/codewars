const myObject = {}

function decodeHiddenData(encodedMessage) {
  let decoded = ''
  return decoded
}

function encodeHiddenData(visibleText, hiddenData) {
  let encodedMessage = ''
  return visibleText + encodedMessage
}

console.log(
  encodeHiddenData('This is visible text. ', 'This is the secret message')
)
